import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#00876E',
      contrastText: '#000',
    },
  },
});

export default theme
